#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    // 1. Inbuilt implementation - trees (same like maps)
    // 2. Only difference - Can add duplicate keys 
    multimap <pair< int, int >, string > m;
    // This isnt used as such
    // if u want to store multiple values corresponding to the same key
    // Do this -> map<int, vector<string>>
    return 0;
}