#include<iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    // time complexity changes in case of iterative datatypes
    map<string, string > map;
    m["abcd"] = "abcd"; //O(string.size * log(n))  
    return 0;
}